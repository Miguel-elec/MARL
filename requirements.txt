# ─── Deep Learning ──────────────────────────────────────────────────────────
torch>=2.2.0
numpy>=1.26.0

# ─── Logging y métricas ─────────────────────────────────────────────────────
matplotlib>=3.8.0
pandas>=2.1.0
tensorboard>=2.15.0

# ─── Utilidades ─────────────────────────────────────────────────────────────
pyyaml>=6.0

# ─── NOTA: SUMO debe instalarse por separado ────────────────────────────────
# Ubuntu/Debian:  sudo apt install sumo sumo-tools sumo-doc
# macOS:          brew install sumo
# Windows:        https://sumo.dlr.de/docs/Installing/index.html
#
# Después de instalar SUMO, añade al PATH:
#   export SUMO_HOME=/usr/share/sumo          (Ubuntu)
#   export SUMO_HOME=/opt/homebrew/share/sumo (macOS)
#
# traci y libsumo vienen incluidos con SUMO (no se instalan por pip).
